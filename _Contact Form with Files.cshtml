@* 
  This file is used as Template-View.
  As superuser the staging version is loaded. This version is for development.
  As a normal user the live version is loaded. 
*@

@{
  string edition = (Dnn.User.IsSuperUser ? "staging" : "live");
}
@RenderPage(edition + "/_Contact Form with Files.cshtml", new { Edition = edition })
